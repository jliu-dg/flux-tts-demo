[project]
name = "saga-smart-city"
version = "0.1.0"
description = "SAGA Smart City Voice AI Demo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask==3.0.0",
    "flask-socketio==5.3.6",
    "janus==1.0.0",
    "python-dotenv==1.0.0",
    "requests==2.32.3",
    "websockets==12.0",
    "gunicorn",
    "eventlet",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
